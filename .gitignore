# Private inbox data — never commit
/kb/*
!/kb/README.md
!/kb/_template.md
/me.md
/tasks.md

# Node (in case a CLI wrapper is added later)
node_modules/
__pycache__/
*.pyc
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db

.vercel
.cloud-data/

# build output (electron-builder)
dist/
desktop/dist/
desktop/release/
desktop/out/
desktop/cleared-chat-*.png
desktop/*-ui.png
desktop/*-voice.png

# triage snapshots (private inbox data)
snapshots/

# Live UI verification can contain private chat content
.brand-preview/

# Local agent handoffs and one-off private exports
*_CONTINUE_FROM_*.md
.claude/launch.json
.claude/settings.json
web/inbox-triage-result.json
web/export-eddie.mjs
docs/cleared-careers-community.md

# our own WhatsApp session (Baileys pairing) — never commit
web/wa-auth/
web/wa-auth-*/
web/wa-auth-current.json
web/wa-store.json
web/contact-aliases.json

# activated license state — local to this machine, never commit
web/.license.json
